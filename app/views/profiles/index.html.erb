<div class="w-full">
  <div class="py-2 flex justify-between items-center">
    <h1 class="font-bold text-4xl"><%= I18n.t 'profiles.properties.title' %></h1>
  </div>

  <%= render "buttons" %>

  <div class="py-2" data-controller="pagy-initializer"><%== pagy_nav(@pagy) %></div>

  <div class="py-2">
    <div class="relative overflow-x-auto shadow-md sm:rounded-lg">
      <table class="w-full border text-sm text-left text-gray-500">
        <thead class="text-xs text-gray-700 bg-gray-200">
        <tr>
          <th scope="col" class="px-6 py-3"><%= sort_link('profiles_path', I18n.t('profiles.attributes.id'), 'id') %></th>
          <th scope="col" class="px-6 py-3"><%= sort_link('profiles_path', I18n.t('profiles.attributes.name'), 'name') %></th>
          <th scope="col" class="px-6 py-3"><%= I18n.t('operator.actions') %></th>
        </tr>
        </thead>
        <tbody>
        <% @profiles.each.with_index do |profile| %>
          <%= render partial: "profile_row", locals: { profile: profile } %>
        <% end %>
        </tbody>
      </table>
    </div>
  </div>
</div>

<%= render "filter" %>
