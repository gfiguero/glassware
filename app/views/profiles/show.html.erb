<div class="w-full">
  <div class="py-2 flex justify-between items-center">
    <h1 class="font-bold text-4xl"><%= I18n.t 'profiles.properties.title' %></h1>
    <h3 class="font-bold text-xl"><%= I18n.t 'profiles.actions.show', id: @profile.id %></h3>
  </div>

  <%= render "buttons" %>

  <%= render @profile %>

</div>
