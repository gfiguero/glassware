<div class="button-container">

  <% if %w[index].include? action_name %>
    <%= button_tag raw(icon('filter') + icon_text(I18n.t('profiles.actions.filter'))), name: 'filter', type: 'button', class: "button-filter", data: { 'drawer-target': "drawer-filter", 'drawer-show': "drawer-filter", 'aria-controls': "drawer-filter" } %>
  <% end %>

  <% unless %w[index].include? action_name %>
    <%= link_to raw(icon('back') + icon_text(I18n.t('profiles.actions.index'))), profiles_path, class: "button-general" %>
  <% end %>

  <% unless %w[new create edit update].include? action_name %>
    <%= link_to raw(icon('new') + icon_text(I18n.t('profiles.actions.new'))), new_profile_path, class: "button-new" %>
  <% end %>

  <% unless %w[new create edit update index].include? action_name %>
    <%= link_to raw(icon('edit') + icon_text(I18n.t('profiles.actions.edit'))), edit_profile_path(@profile), class: "button-edit" %>
  <% end %>

</div>
