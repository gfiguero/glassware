<div class="w-full">
  <div class="py-2 flex justify-between items-center">
    <h1 class="font-bold text-4xl"><%= I18n.t 'maps.properties.title' %></h1>
    <h3 class="font-bold text-xl"><%= I18n.t 'maps.actions.new', id: @map.id %></h3>
  </div>

  <%= render "buttons" %>

  <%= render "form", map: @map %>

</div>
