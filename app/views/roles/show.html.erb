<div class="w-full">
  <div class="py-2 flex justify-between items-center">
    <h1 class="font-bold text-4xl"><%= I18n.t 'roles.properties.title' %></h1>
    <h3 class="font-bold text-xl"><%= I18n.t 'roles.actions.show', id: @role.id %></h3>
  </div>

  <%= render "buttons" %>

  <%= render @role %>

</div>
