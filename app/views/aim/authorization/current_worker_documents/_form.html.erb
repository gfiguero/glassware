<%= form_with(model: aim_authorization_current_worker_document) do |form| %>
  <% if aim_authorization_current_worker_document.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(aim_authorization_current_worker_document.errors.count, "error") %> prohibited this aim_authorization_current_worker_document from being saved:</h2>

      <ul>
        <% aim_authorization_current_worker_document.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :request_workerable_worker_id, style: "display: block" %>
    <%= form.number_field :request_workerable_worker_id %>
  </div>

  <div>
    <%= form.label :worker_document_id, style: "display: block" %>
    <%= form.number_field :worker_document_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
