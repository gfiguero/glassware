<%= form_with(model: aim_checklist_checklist_categories_custom_report) do |form| %>
  <% if aim_checklist_checklist_categories_custom_report.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(aim_checklist_checklist_categories_custom_report.errors.count, "error") %> prohibited this aim_checklist_checklist_categories_custom_report from being saved:</h2>

      <ul>
        <% aim_checklist_checklist_categories_custom_report.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :checklist_category_id, style: "display: block" %>
    <%= form.number_field :checklist_category_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
