<%= form_with(model: aim_checklist_checklist_tag_grade_weight) do |form| %>
  <% if aim_checklist_checklist_tag_grade_weight.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(aim_checklist_checklist_tag_grade_weight.errors.count, "error") %> prohibited this aim_checklist_checklist_tag_grade_weight from being saved:</h2>

      <ul>
        <% aim_checklist_checklist_tag_grade_weight.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :checklist_id, style: "display: block" %>
    <%= form.number_field :checklist_id %>
  </div>

  <div>
    <%= form.label :tag_id, style: "display: block" %>
    <%= form.number_field :tag_id %>
  </div>

  <div>
    <%= form.label :grade_weight, style: "display: block" %>
    <%= form.text_field :grade_weight %>
  </div>

  <div>
    <%= form.label :grade, style: "display: block" %>
    <%= form.text_field :grade %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
