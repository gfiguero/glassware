<%= form_with(model: aim_checklist_checklist_interface_group) do |form| %>
  <% if aim_checklist_checklist_interface_group.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(aim_checklist_checklist_interface_group.errors.count, "error") %> prohibited this aim_checklist_checklist_interface_group from being saved:</h2>

      <ul>
        <% aim_checklist_checklist_interface_group.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :group_id, style: "display: block" %>
    <%= form.number_field :group_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
